export type MagnifierState = {
  leftPercent: number;
  timeSec: number;
  visible: boolean;
};

export type LoopSegment = {
  id: string;
  start: number;
  end: number;
  color: string;
  label: string;
};

