apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: music-man
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/snothub/literate-octo-guacamole.git
    targetRevision: feature/db
    path: kubernetes/helm-chart

    helm:
      valueFiles:
      - values.yaml
      values: |
        # Override Docker Hub username
        frontend:
          image:
            repository: danijels/music-man
            tag: v1.1.0  # Updated automatically by GitHub Actions on release
          env:
            spotifyClientId: "011c5f27eef64dd0b6f65ca673215a58"
            apiUrl: "http://backend:4000"

        backend:
          image:
            repository: danijels/music-man-api
            tag: v1.1.0  # Updated automatically by GitHub Actions on release

        # Ingress configuration (optional)
        ingress:
          enabled: false
          hosts:
            - host: music-man.example.com
              paths:
                - path: /
                  pathType: Prefix
                  backend: frontend
                - path: /api
                  pathType: Prefix
                  backend: backend

      parameters:
      - name: secrets.databasePassword
        value: "change-me-in-production"

  destination:
    server: https://kubernetes.default.svc
    namespace: music-man

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false

    syncOptions:
    - CreateNamespace=true
    - Validate=true
    - ServerSideApply=false
    - Replace=false
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  ignoreDifferences:
  - group: apps
    kind: Deployment
    jsonPointers:
    - /spec/replicas
